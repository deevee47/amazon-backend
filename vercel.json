{
  "buildCommand": "bun build ./src/app.ts --outfile=./api/index.js --target=node --format=esm",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
